---
export interface Props {
  src: string;
  title?: string;
}

const { src, title } = Astro.props;
---

<div class="audio-player" title={title}>
  <audio controls src={src}>
    Your browser does not support the audio element.
  </audio>
</div>
